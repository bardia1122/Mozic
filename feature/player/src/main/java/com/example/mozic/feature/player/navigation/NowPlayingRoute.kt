package com.example.mozic.feature.player.navigation

import kotlinx.serialization.Serializable

/** No params — the screen renders whatever `PlayerController.state` currently holds. */
@Serializable
data object NowPlayingRoute
